import fs from 'fs';
import Jimp from 'jimp';

export default class Obj {
    
    static load() {
        throw new Error("not implemented yet");
    }
    
    static saveMaterial(colorModel, filename, cb) {

        //2. prepare mtl

        //3. prepare png

    }
    
    static saveObj(verticesModel, filename, cb) {
        //1. prepare obj

        //4. save all
    }
    
}